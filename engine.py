# The most functional file "engine/config/operating", also most important

# start() will be used to launch the CLI booking demo
def start(): # Initializing the function
    print(True) # Testing if it prints the boolean "True", to determine a proper result