# Import the start function from the engine module
from engine import start as main # Set alias to "main"

# If this file is run, and it has the name "main.py", then call main()
if __name__ == "__main__": # If this statement is true, do the following:
    main() # main takes no arguments, because it has no parameters